node_modules
.DS_Store
bun.lockb
*.log
.staticrypt.json
docs/projects.json
.tmp-fitness/

# build state — must never publish (password-oracle risk, see preed-ee-src/build.ts)
docs/.build-manifest.json
